/**
 * Sensor controller module
 */

/**
 * Sensor controller class.
 */
export class SensorControler {
  updateSensors(req, res, next) {
    console.log(req.body)

    // store data

    res.status(200).send()
  }
}
